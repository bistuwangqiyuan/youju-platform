# Next.js on Netlify: @netlify/plugin-nextjs sets the effective publish/output;
# publish = ".next" documents the Next build output for tooling that reads this file.
[build]
  command = "npm ci && npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"
  # Netlify sets NODE_ENV=production during install; devDependencies are required for Tailwind/PostCSS and the Next plugin.
  NPM_CONFIG_PRODUCTION = "false"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
